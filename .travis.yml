language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"
  - "3.7-dev"

before_install:
  - sudo apt-get install geckodriver
install:
  - 'pip install pipenv'
  - 'pipenv install -d'

script: "pipenv run pytest --pylama"
